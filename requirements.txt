# Core (required)
pydantic>=2.0

# Recommended — improves correctness of normalization. The code degrades to
# regex/heuristic fallbacks if these are absent, so the sample run still works.
phonenumbers>=8.13
python-dateutil>=2.8

# Optional — only needed for non-.txt resumes or live GitHub fetching.
pdfplumber>=0.11     # PDF resumes
python-docx>=1.1     # DOCX resumes
requests>=2.31       # live GitHub fetch (fixtures are used by default)

# Tests
pytest>=8.0
